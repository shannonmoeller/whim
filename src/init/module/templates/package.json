{
  "name": "<%= slug %>",
  "version": "<%= version %>",
  "description": "<%= description %>",
  "keywords": [],
  "license": "MIT",
  "repository": "<%= repoUser %>/<%= slug %>",
  "homepage": "https://<%= repoHost %>.com/<%= repoUser %>/<%= slug %>#readme",
  "author": {
    "name": "<%= authorName %>",
    "email": "<%= authorEmail %>",
    "url": "<%= authorUrl %>"
  },
  "main": "src/index.js",
  "files": [
    "*.js",
    "dist"
  ],
  "dependencies": {},
  "devDependencies": {
    "<%= pkg.name %>": "^<%= pkg.version %>"
  },
  "scripts": {
    "start": "<%= pkg.name %> make",
    "pretest": "<%= pkg.name %> lint",
    "test": "<%= pkg.name %> test",
    "report": "<%= pkg.name %> test report"
  },
  "engines": {
    "node": "<%= pkg.engines.node %>"
  }
}
