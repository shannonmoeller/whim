{
  "name": "<%= slug %>",
  "version": "<%= version %>",
  "description": "<%= description %>",
  "keywords": [],
  "license": "UNLICENSED",
  "repository": "<%= repoUser %>/<%= slug %>",
  "homepage": "http<%= ssl ? 's' : '' %>://<%= domain %>",
  "author": {
    "name": "<%= authorName %>",
    "email": "<%= authorEmail %>",
    "url": "<%= authorUrl %>"
  },
  "main": false,
  "private": true,
  "dependencies": {
    "babel-polyfill": "^6.7.2",
    "deduce": "0.1.3",
    "document-register-element": "^0.5.4",
    "dom4": "^1.7.0",
    "form-serialize": "^0.7.1",
    "keyboardevent-key-polyfill": "^1.0.1",
    "morphdom": "^1.1.2",
    "reset-css": "^2.0.2011012603",
    "svg4everybody": "^2.0.3",
    "whatwg-fetch": "^0.11.0"
  },
  "devDependencies": {
    "<%= pkg.name %>": "^<%= pkg.version %>"
  },
  "scripts": {
    "start": "<%= pkg.name %> make",
    "pretest": "<%= pkg.name %> lint",
    "test": "<%= pkg.name %> test",
    "report": "<%= pkg.name %> test report",
    "serve": "<%= pkg.name %> make server -mv"
  },
  "engines": {
    "node": "<%= pkg.engines.node %>"
  }
}
